<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rush.cocktailrush">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CocktailRush">
        <activity android:name=".MuleActivity"></activity>
        <activity android:name=".NojitoActivity" />
        <activity android:name=".MaryActivity" />
        <activity android:name=".BelliniActivity" />
        <activity android:name=".SangriaActivity" />
        <activity android:name=".CranberryActivity" />
        <activity android:name=".Cocktails" />
        <activity android:name=".SignCocktails" />
        <activity android:name=".LoginCocktails">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>